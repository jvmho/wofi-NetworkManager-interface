# wofi based Wi-Fi connection interface for NetworkManager
## About
This is the interface for NetworkManager based on wofi.<br>
Connect to Wi-Fi without using terminal!
## Installation and use
Make `script.sh` file executable using this command:
```
chmod +x script.sh
```
And run it with `./script.sh`
